[pytest]
pythonpath = .
testpaths = Fish/Other
norecursedirs = .* build dist CVS _darcs {arch} *.egg venv .venv .micromamba .cache .local 3 4 5 6 7 8 B C D E
markers =
    gui: tests that require a GUI / Qt
    unit: logic-only tests